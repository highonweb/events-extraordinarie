<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="../../css/home.css" />
    <script
      src="https://kit.fontawesome.com/c5b9bcd338.js"
      crossorigin="anonymous"></script>
  </head>
  <body>
    <div id="header">
        <ul class="navbar"></ul>
        <span style="font-size: 30px; cursor: pointer;" ><i class="fas fa-calendar-alt"></i> Events Extraordinaire..
            <li class="nav" ><a class="nava" href="/logout"><i class="fas fa-sign-out-alt"></a></i></li>
          </ul></span>
    </div>
    <br><br><br><br>
    <table border="1">
        <tr>
            <th>Name</th>
            <th>Co-Attendees</th>
            <th>Meal</th>
            <th>Present</th>
            <th>Absent</th>
        </tr>
        <% for( let index = 0; index < pevent.length; index++ ) { %>
        <tr>
            <td><%= pevent[index].user.username.toUpperCase() %></td> </td>
            <td><%= pevent[index].comingwith %> </td>
            <td><%= pevent[index].meal %> </td>
            <td><a href="/came/<%= pevent[index].user.username.toUpperCase() %>/<%= event._id %> "><i class="fas fa-check"></i></a></td>
            <td><a></a><i class="fas fa-times"></i></td>
        </tr>
        <% } %>
    </table>
    <br><br><br><br>
    <div class="nsopa">
<ul>
        <h1>In The Hall</h1>
    <% for( let index = 0; index < event.came.length; index++ ) { %>
        <li class="invites" style="text-align: left;"><%= event.came[index] %> </li>
        <% } %> 

    </ul>

    </div>
    
    
